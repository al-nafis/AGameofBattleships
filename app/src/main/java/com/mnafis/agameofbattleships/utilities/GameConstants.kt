package com.mnafis.agameofbattleships.utilities

enum class GameDifficulty(val value: String) {
    EASY("easy"),
    NORMAL("normal")
}

enum class AudioStatus(val value: Boolean) {
    ON(true),
    OFF(false)
}
